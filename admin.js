const config=window.PHOTO_SITE_CONFIG;const configured=config&&!config.supabaseUrl.includes('YOUR-PROJECT')&&!config.supabaseAnonKey.includes('YOUR-SUPABASE');const client=configured?supabase.createClient(config.supabaseUrl,config.supabaseAnonKey):null;const loginPanel=document.querySelector('#login-panel');const managerPanel=document.querySelector('#manager-panel');const filesInput=document.querySelector('#photo-files');const dropZone=document.querySelector('#drop-zone');const loginMessage=document.querySelector('#login-message');let albums=[];
function slugify(v){return v.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}function showMessage(el,msg,error=false){el.textContent=msg;el.classList.toggle('error',error)}function setLoggedIn(ok){loginPanel.classList.toggle('hidden',ok);managerPanel.classList.toggle('hidden',!ok);if(ok)refreshAlbums()}
if(!configured){showMessage(loginMessage,'Supabase is not configured yet.',true)}else{client.auth.getSession().then(({data,error})=>{if(error)showMessage(loginMessage,error.message,true);setLoggedIn(Boolean(data?.session))});client.auth.onAuthStateChange((_e,s)=>setLoggedIn(Boolean(s)))}
document.querySelector('#login-form').addEventListener('submit',async e=>{e.preventDefault();showMessage(loginMessage,'Signing in…');const{data,error}=await client.auth.signInWithPassword({email:document.querySelector('#email').value.trim(),password:document.querySelector('#password').value});if(error)return showMessage(loginMessage,error.message,true);setLoggedIn(Boolean(data?.session))});document.querySelector('#logout-button').addEventListener('click',()=>client.auth.signOut());
document.querySelector('#album-form').addEventListener('submit',async e=>{e.preventDefault();const title=document.querySelector('#album-title').value.trim();const{error}=await client.from('albums').insert({title,slug:`${slugify(title)}-${Date.now().toString().slice(-6)}`,description:document.querySelector('#album-description').value.trim(),event_date:document.querySelector('#album-date').value||null,expires_at:document.querySelector('#album-expiry').value||null,is_public:document.querySelector('#album-public').checked,watermark_enabled:document.querySelector('#album-watermark').checked,watermark_type:document.querySelector('#album-watermark-type').value});if(error)return alert(error.message);e.target.reset();document.querySelector('#album-public').checked=true;refreshAlbums()});
async function refreshAlbums(){const{data,error}=await client.from('albums').select('id,title,event_date,expires_at,is_public,cover_url,created_at,watermark_enabled,watermark_type,photos(count)').order('created_at',{ascending:false});if(error){showMessage(loginMessage,`Database error: ${error.message}`,true);return}albums=data||[];document.querySelector('#album-select').innerHTML='<option value="">Select an album</option>'+albums.map(a=>`<option value="${a.id}">${a.title}</option>`).join('');const list=document.querySelector('#admin-album-list');list.innerHTML=albums.map(a=>`<article class="admin-album-row"><div class="admin-thumb" style="background-image:url('${a.cover_url||''}')"></div><div><strong>${a.title}</strong><small>${a.event_date||'No date'} · ${a.photos?.[0]?.count||0} photos · ${a.is_public?'Public':'Private'}${a.expires_at?` · expires ${a.expires_at}`:''}</small></div><div class="watermark-settings"><select data-watermark-type="${a.id}" aria-label="Watermark style"><option value="proof" ${a.watermark_type==='proof'?'selected':''}>PROOF</option><option value="logo" ${a.watermark_type==='logo'?'selected':''}>MS logo</option></select><button class="watermark-toggle ${a.watermark_enabled?'active':''}" data-watermark-toggle="${a.id}" type="button">${a.watermark_enabled?'Watermark on':'Watermark off'}</button></div><button class="danger-text" data-delete="${a.id}">Delete</button></article>`).join('')||'<p>No albums yet.</p>';list.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',()=>deleteAlbum(b.dataset.delete)));list.querySelectorAll('[data-watermark-toggle]').forEach(b=>b.addEventListener('click',()=>toggleWatermark(b.dataset.watermarkToggle)));list.querySelectorAll('[data-watermark-type]').forEach(s=>s.addEventListener('change',()=>setWatermarkType(s.dataset.watermarkType,s.value)))}
async function toggleWatermark(id){const album=albums.find(a=>a.id===id);const{error}=await client.from('albums').update({watermark_enabled:!album.watermark_enabled}).eq('id',id);if(error)alert(error.message);else refreshAlbums()}async function setWatermarkType(id,type){const{error}=await client.from('albums').update({watermark_type:type}).eq('id',id);if(error)alert(error.message);else refreshAlbums()}
async function deleteAlbum(id){if(!confirm('Delete this album and all of its photos?'))return;const{data:photos}=await client.from('photos').select('storage_path').eq('album_id',id);if(photos?.length)await client.storage.from('photos').remove(photos.map(p=>p.storage_path));const{error}=await client.from('albums').delete().eq('id',id);if(error)alert(error.message);else refreshAlbums()}
['dragenter','dragover'].forEach(n=>dropZone.addEventListener(n,e=>{e.preventDefault();dropZone.classList.add('dragging')}));['dragleave','drop'].forEach(n=>dropZone.addEventListener(n,e=>{e.preventDefault();dropZone.classList.remove('dragging')}));dropZone.addEventListener('drop',e=>{filesInput.files=e.dataTransfer.files;updateFileSummary()});filesInput.addEventListener('change',updateFileSummary);function updateFileSummary(){const c=filesInput.files.length,s=[...filesInput.files].reduce((t,f)=>t+f.size,0);document.querySelector('#file-summary').textContent=c?`${c} photos selected · ${(s/1024/1024).toFixed(1)} MB`:'No photos selected'}
document.querySelector('#upload-form').addEventListener('submit',async e=>{e.preventDefault();const albumId=document.querySelector('#album-select').value,files=[...filesInput.files];if(!albumId||!files.length)return alert('Choose an album and at least one photo.');const status=document.querySelector('#upload-status'),bar=document.querySelector('#progress-bar'),text=document.querySelector('#progress-text');status.classList.remove('hidden');let completed=0,firstUrl=null;const queue=[...files],workers=Array.from({length:Math.min(4,queue.length)},async()=>{while(queue.length){const file=queue.shift(),safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'-'),path=`${albumId}/${crypto.randomUUID()}-${safe}`;const{error:uploadError}=await client.storage.from('photos').upload(path,file,{cacheControl:'31536000',upsert:false});if(!uploadError){const{data}=client.storage.from('photos').getPublicUrl(path);firstUrl||=data.publicUrl;await client.from('photos').insert({album_id:albumId,storage_path:path,image_url:data.publicUrl,thumbnail_url:`${data.publicUrl}?width=900&quality=80`,sort_order:completed})}completed++;bar.style.width=`${Math.round(completed/files.length*100)}%`;text.textContent=`Uploaded ${completed} of ${files.length} photos`}});await Promise.all(workers);const album=albums.find(a=>a.id===albumId);if(firstUrl&&!album?.cover_url)await client.from('albums').update({cover_url:firstUrl}).eq('id',albumId);filesInput.value='';updateFileSummary();text.textContent=`Complete — ${files.length} photos uploaded.`;refreshAlbums()});